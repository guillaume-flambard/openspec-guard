import { describe, expect, it } from 'vitest';

describe('pen name', () => {
  it('rejects a pen name shorter than two characters', () => {
    expect(true).toBe(true);
  });

  it('accepts a pen name of twenty-four characters', () => {
    expect(true).toBe(true);
  });
});
