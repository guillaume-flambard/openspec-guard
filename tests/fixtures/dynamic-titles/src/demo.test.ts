import { expect, it, describe } from 'vitest';

const subject = 'email';

describe(subject, () => {
  it('rejects an invalid email address', () => {
    expect(true).toBe(true);
  });
});

it(`checks ${subject}`, () => {
  expect(true).toBe(true);
});
