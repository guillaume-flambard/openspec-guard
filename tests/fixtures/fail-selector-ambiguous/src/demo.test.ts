import { describe, expect, it } from 'vitest';

describe('login', () => {
  it('empty field', () => {
    expect(true).toBe(true);
  });
});

describe('signup', () => {
  it('empty field', () => {
    expect(true).toBe(true);
  });
});
